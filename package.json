{
  "name": "cta-artifact-eval",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Comitê Técnico de Artefatos (CTA) — site de avaliação de artefatos (SBRC/SBSeg), PT/EN/ES.",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "start": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.3"
  }
}
